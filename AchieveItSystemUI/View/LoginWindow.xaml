<Window x:Class="AchieveItSystemUI.View.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:AchieveItSystemUI.View"
        mc:Ignorable="d"
        Title="LoginWindow" Height="540" Width="320"
        WindowStyle="None" 
        AllowsTransparency="True"
        Background="Transparent"
        WindowStartupLocation="CenterScreen"
        x:Name="MyLoginWindow">

    <Border CornerRadius="15" Background="White" Padding="12">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="1*"/>
                <RowDefinition Height="1*"/>
                <RowDefinition Height="6*"/> 
                <RowDefinition Height="2*"/>
            </Grid.RowDefinitions>
            <Button Width="20" Height="20" Background="Transparent" BorderBrush="Transparent" 
                    HorizontalAlignment="Right" VerticalAlignment="Top" Grid.Row="0"
                    Command="{Binding CancelCommand}" CommandParameter="{Binding ElementName=MyLoginWindow}">
                <TextBlock 
                FontFamily="{StaticResource SymbolThemeFontFamily}" 
                Text="&#xE711;" 
                FontSize="16" 
                HorizontalAlignment="Center" 
                VerticalAlignment="Center"/>
            </Button>
            <Grid Grid.Row="1">
                <Grid.RowDefinitions>
                    <RowDefinition/>
                    <RowDefinition/>
                </Grid.RowDefinitions>
                <TextBox Grid.Row="0" Text="{Binding CurrentUserInfo.UserCode}" Grid.ColumnSpan="2" Margin="0,0,0,26" Grid.RowSpan="2"></TextBox>
                <TextBox Grid.Row="1" Text="{Binding CurrentUserInfo.UserPassword}" Grid.ColumnSpan="2"></TextBox>
            </Grid>
            <Grid Grid.Row="3">
                <Button Content="登录" Command="{Binding LoginCommand}" CommandParameter="{Binding ElementName=MyLoginWindow}" ></Button>
            </Grid>
        </Grid>
    </Border>
</Window>
